<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="ir_cron_sms_scheduled" model="ir.cron">
            <field name="name">SMS Marketing: Enviar campanas programadas</field>
            <field name="model_id" search="[('model','=','interconectados.sms.campaign')]"/>
            <field name="state">code</field>
            <field name="code">model._cron_send_scheduled()</field>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active">True</field>
            <field name="priority">10</field>
            <field name="user_id" ref="base.user_root"/>
        </record>
    </data>
</odoo>
